import React from 'react'
import TodoDay from './TodoTime'

const TodoCard = () => {
    return (
        <section className="p-4 bg-white max-w-xl w-full">
            <TodoDay />
        </section>
    )
}

export default TodoCard